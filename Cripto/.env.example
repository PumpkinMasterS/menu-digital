# Backend environment example
PORT=3000
NODE_ENV=production
# Redis, TimescaleDB, Mongo URIs to be added later
REDIS_URL=redis://localhost:6379
TIMESCALE_URL=postgres://postgres:postgres@localhost:5432/cripto
MONGO_URL=mongodb://localhost:27017/cripto

# Risk management (choose one mode)
# Absolute daily drawdown in USD (takes precedence if set)
# MAX_DAILY_DRAWDOWN_USD=10
# Or percentage of base equity (e.g., 1 = 1%)
# MAX_DAILY_DRAWDOWN_PCT=1
# Base equity used to compute percentage limit (USD)
# BASE_EQUITY_USD=1000